server:
  port: 80

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:hello-querydsl;
    username: abc
    password: abc
  h2:
    console:
      path: /h2
      enabled: true
  jpa:
    show-sql: true